<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="vao.wallet">

    <dist:module dist:instant="true" />


    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/vao_app"
        android:label="@string/app_name"
        android:roundIcon="@drawable/vao_app"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity"></activity>
        <activity android:name=".LoginSubActivity"></activity>
        <activity android:name=".JoinActivity"></activity>
        <activity android:name=".TwoWayPasswordAcitivty"></activity>
        <activity android:name=".MainSubActivity"></activity>
        <activity
            android:name=".DimLoadingActivity"
            android:theme="@style/Theme.Transparent">
        </activity>
        <activity android:name=".SendWhoActivity"></activity>
        <activity android:name=".NoticeAcitvity"></activity>
    </application>

</manifest>